# The scanner suite (.claude/skills/review-site/tools/) is pure Python standard
# library and needs nothing here. This file covers the Word report builder
# (build_exec_report.py) and its test suite (test_exec_report.py). Its
# third-party dependencies are python-docx (always) and matplotlib (used by
# report_charts.py to render the quarterly-trend charts in reports that reach
# three or more quarters of history; the builder raises with an install hint if
# a trend needs charts and matplotlib is absent).
python-docx>=1.1,<2
matplotlib>=3.7,<4
