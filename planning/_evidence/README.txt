This folder holds working material for the website review: screenshots, raw
header and DNS dumps, page notes, and exec_report_data.json.

It is safe to delete the contents between runs. The review skill recreates
whatever it needs. This README exists only so the folder is preserved inside
the zip, since an archive cannot carry an empty folder.
